{
  "name": "thinkio-rebuild",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Minimal governed task kernel/runtime rebuild for ThinkIO.",
  "scripts": {
    "test": "node --test tests/*.test.ts",
    "validate:cue": "node runtime/validate-schemas.ts",
    "check": "npm run validate:cue && node --test tests/*.test.ts"
  },
  "engines": {
    "node": ">=23.6.0"
  }
}
