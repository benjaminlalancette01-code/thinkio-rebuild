# TASK-009: Refresh Runtime Flow Visual Support

Refresh `views/thinkio-runtime-flow.json` so it matches the completed TASK-001 through TASK-008 kernel state.

This is a maintenance task from the identity audit, not a legacy import.

## Subtasks

- Replace stale TASK-008 candidate references with done/accepted state.
- Remove stale notes saying view generation is missing.
- Add current `update-views` runtime flow.
- Keep the file as visual support only, not authority.

## Required Evidence

- `runtime-flow-drift-identified`
- `runtime-flow-view-refreshed`
- `runtime-flow-test-pass`

