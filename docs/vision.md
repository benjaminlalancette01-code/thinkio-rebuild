# Vision

ThinkIO is a governed task/runtime layer for local project work.

It is not an AI model and not merely a second brain. Its job is to control task authority, mode-scoped context, dependencies, deferred work, artifact lineage, evidence, approvals, checkpoints, runtime gating, and safe local execution.

The core primitive is the governed task object.

